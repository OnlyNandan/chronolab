COMPLETED:
- [P0] Synthetic data fixtures generation (PDFs).
- [P0] LocalStack infrastructure (S3 bucket, DynamoDB table).
- [P0] AI Pipeline Phase A (Vision/Text fallback extraction).
- [P0] AI Pipeline Phase B (Strict Canonicalization & Schema conforming).
- [P0] FastAPI Server layer mapping to DynamoDB.
- [P0] React + Vite UI with Recharts Timeline (Premium CSS).
- [P0] Demo script end-to-end integration test.
- [P1] "New Doctor Mode" strict text-only trend generation agent.
- [P2] Minimal Cedar Policy Auth/RBAC layer.
- [P2] Medication CRUD forms or NLP query box in React frontend.
- [POLISH] Voice-to-Text NLP Query UI using Web Speech API.
- [POLISH] Live Doctor Mode Insights API & React component integration.
- [WORLD-CLASS] Real-Time Collaboration via FastAPI WebSockets.
- [WORLD-CLASS] HealthTech Interoperability via FHIR Export Endpoint.
- [WORLD-CLASS] Premium Startup-Level UI (Animated Gradient Mesh).
- [1ST-PLACE] Interactive AI Timeline Assistant (RAG Chatbot).

- [MIGRATION P0] Config scaffolding: requirements.txt, .env.example, .gitignore, scripts/bootstrap.sh stub.
- [MIGRATION P1] Real S3 + DynamoDB via backend/config.py (AWS_MODE=local|cloud) and bootstrap.sh provisioning.
- [MIGRATION P2] Bedrock provider abstraction (backend/providers.py) alongside Ollama; unit conversion moved to deterministic Python (backend/unit_conversion.py, 8 tests); silent vision->text fallback now logs at WARNING.
- [MIGRATION P3] Real Cedar auth via Amazon Verified Permissions: cedar/*.cedar policies, auth_middleware.py rewritten around IsAuthorizedWithToken, Cognito+AVP bootstrap.sh provisioning, frontend login screen + token-based auth.
- [MIGRATION P4] Deploy: backend/Dockerfile (non-root, tested for real with `docker build`+`docker run` against moto_server), deploy/docker-compose.yml + Caddyfile (TLS via DuckDNS, WS reverse-proxy), bootstrap.sh EC2 provisioning (least-privilege IAM instance profile, security group, key pair, AMI lookup, user-data) + Amplify app/branch scaffold, scripts/deploy.sh (scp+ssh backend push, Amplify manual zip deploy), CORS_ALLOWED_ORIGINS + WebSocket origin check in server.py.
- [MIGRATION P9] Handoff basics: HANDOFF.md (command sequence, expected output per step, .env values, test credentials, troubleshooting table, EC2-stop reminder) and scripts/teardown.sh (deletes everything bootstrap.sh created, in reverse order, with a confirmation prompt). teardown.sh dry-run tested against a simulated post-bootstrap .env — real file deletion (the local .pem) verified for real, AWS calls via fake-aws-CLI-stub.

IN PROGRESS:
- AWS migration (see MIGRATION_PLAN.md, local copy only, not committed) — 24-hour mode, Phases 0-4 in scope. All four phases + Phase 9 handoff basics now code-complete.
- Everything is UNTESTED against a real AWS account — no AWS credits yet. Verified via: LocalStack/moto (Phases 0-1), fake-provider wiring tests (Phase 2), mocked-AVP-client + fake-aws-CLI-stub dry run (Phase 3), a REAL `docker build`+`docker run` of the backend image against moto_server (Phase 4), a fake-aws-CLI-stub dry run of the EC2/IAM/Amplify bootstrap.sh logic including a real idempotency re-run (Phase 4), and a fake-aws-CLI-stub dry run of teardown.sh. Needs a real dry run per MIGRATION_PLAN.md Phase 9 the moment AWS access exists.

NEXT:
- Real AWS dry run of Phases 1-4 once account credits land (this is the big remaining unknown).
- Phase 8 submission hygiene: update/delete stale IMPLEMENTATION_PLAN.md/BACKEND_STRUCTURE.md, validate FHIR bundle, writeup, demo video.
- tests/test_endpoints.sh is now stale (still sends the old X-User-Role/X-User-Id headers from before Phase 3's Bearer-token auth) — not fixed, flagged only.

KNOWN BUGS:
- `llava:7b` vision model extraction fails if model isn't pulled in Ollama (fails over to text successfully, now logs at WARNING instead of print()).
- Unverified assumption (Phase 3): AVP's Cognito identity source is assumed to map `custom:role`/`custom:patient_id` token claims directly to `principal.role`/`principal.patient_id` with no extra schema config. Standard documented behavior, but never exercised against a real pool — see the note bootstrap.sh prints after Cognito/AVP provisioning.
